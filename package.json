{
  "name": "zed-cea",
  "private": true,
  "scripts": {
    "generate": "cd grammars/cea && ${TREE_SITTER:-tree-sitter} generate",
    "test": "cd grammars/cea && ${TREE_SITTER:-tree-sitter} test"
  },
  "devDependencies": {
    "tree-sitter-cli": "0.26.11"
  }
}
