{
  "name": "zed-cea",
  "private": true,
  "scripts": {
    "generate": "cd grammars/cea && tree-sitter generate",
    "test": "cd grammars/cea && tree-sitter test"
  },
  "devDependencies": {
    "tree-sitter-cli": "0.26.11"
  }
}
